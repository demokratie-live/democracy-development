apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: local-storage
  namespace: dapr-system
spec:
  type: bindings.localstorage
  version: v1
  metadata:
    - name: rootPath
      value: /tmp/dapr
