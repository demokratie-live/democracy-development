name: Docker – index-sync-bundestagio

on:
  push:
    paths:
      - "services/cron-jobs/index-sync-bundestagio/**"
    # branches:
    #   - master

jobs:
  build:
    name: Docker – index-sync-bundestagio
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Read package.json
        uses: tyankatsu0105/read-package-version-actions@v1
        id: package-version
        with:
          path: "./services/cron-jobs/index-sync-bundestagio"

      - uses: docker/build-push-action@v1
        with:
          username: ${{ github.actor }}
          password: ${{ github.token }}
          registry: docker.pkg.github.com
          repository: ${{ github.repository }}/index-sync-bundestagio
          path: ./services/cron-jobs/index-sync-bundestagio
          tags: ${{ steps.package-version.outputs.version }}
